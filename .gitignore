# Game files you supply yourself: code.bin and exheader.bin dumps per version.
bin/*.bin

# Build output
code/build/
code/*.elf
code/*.map
code/*.ips
code/*.bin
code/*.sym
*.o

# Release packaging
release/

# Python bytecode, written whenever a tool is imported rather than run
__pycache__/
*.pyc
